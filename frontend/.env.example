# Copied to `.env` for local `npm run dev` / `npm run build`.
# In Docker Compose, pass as build-arg so the browser calls the API on your host.
# Dev server URL defaults to http://localhost:5180 (Vite `server.port` in vite.config.ts).

PUBLIC_API_BASE_URL=http://localhost:8800
